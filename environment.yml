name: hovernet
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - ipykernel
  # remove openslide for window because you need to install manually
  # - openslide
  - pip:
    - -r requirements.txt
    - openslide-python

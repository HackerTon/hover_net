

optimizer_kwargs:
  'lr' : 1.0e-4
  'betas' : [0.9, 0.999]
  'weight_decay' : 0.0

loader_kwargs:
  # phase 1
  # train: { 'batch_size' : 6, 'nr_procs' : 8}
  # infer: { 'batch_size' : 6, 'nr_procs' : 8}
  # phase 2
  train: { 'batch_size' : 1, 'nr_procs' : 4}
  infer: { 'batch_size' : 2, 'nr_procs' : 4} # batch_size for infer must be at least 2 because accumulation will fail

model_kwargs:
  num_types: 6 # conic
  pretrained_backbone: 'exp_output/local/resnet50-0676ba61.pth'
